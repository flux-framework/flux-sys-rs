[package]
name = "flux-sys"
version = "0.3.0"
authors = ["Tom Scogland <scogland1@llnl.gov>"]
edition = "2021"
links = "flux"

[build-dependencies]
bindgen = "0.69"
pkg-config = "0.3"

[features]
default = ["core", "idset", "hostlist"]
core = []
jobtap = []
idset = []
hostlist = []
# sched = []
